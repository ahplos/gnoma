View the manifests that generated by Gnoma Chart from the `helm upgrade --install` command:
+
```
$ helm get manifest -n gnoma-chart-demo gnoma-chart-demo
```
+
.**Output**
[%collapsible]
====
```
include::helm-manifests/{helm-manifest-file}.adoc[]
```
====